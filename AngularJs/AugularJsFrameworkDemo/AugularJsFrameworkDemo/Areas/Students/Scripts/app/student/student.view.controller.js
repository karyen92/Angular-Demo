(function (window, angular, undefined) {
    'use strict';

    angular.module('demo.student.controllers').controller('StudentViewController',

    ['$rootScope', '$scope', '$filter', '$window', 'ephemeral', '$state', '$http',
        function ($rootScope, $scope, $filter, $window, ephemeral, $state, $http) {
            
        }]);

})(window, window.angular);
