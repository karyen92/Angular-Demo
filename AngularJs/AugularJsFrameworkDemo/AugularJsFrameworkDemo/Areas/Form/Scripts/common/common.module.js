(function (window, angular, undefined) {
    'use strict';

    angular.module('form.common', [
        'form.common.services']);

    angular.module('form.common.services',[]);

})(window, window.angular);
